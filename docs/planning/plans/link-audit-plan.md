# 全书链接审计修复计划

> **给代理工作者：** 使用 subagent-driven-development 或 executing-plans 逐任务实现此计划。

**目标：** 对 `src/` 下 56 个 .md 文件进行全面内部链接检查，修复所有断链、编号错位、链接文字不一致、sidebar 不同步等问题，最终验证零残留。

**范围：** 仅内部链接（`.md` 路径、`#` 锚点、`§X.Y` 引用）。不检查外部 URL。

**技术栈：** grep、ast-grep、explore agent、file read/validate

---

### 任务 1：并行审计（4 路同时进行）

- [ ] **步骤 1.1：断链检测** — 提取所有内部 `.md` 链接路径，验证目标文件存在

- [ ] **步骤 1.2：§X.Y 编号审计** — 提取所有 `§X.Y` 引用，对照各章 article 数验证范围合法性

- [ ] **步骤 1.3：链接文字一致性** — 链接文字 vs 目标文件 H1 标题是否一致

- [ ] **步骤 1.4：Sidebar 同步检查** — glob 文件列表 vs sidebar 条目是否一一对应

### 任务 2：修复（按问题类别）

- [ ] **步骤 2.1：修复断链** — 对每条无效路径修正为正确路径

- [ ] **步骤 2.2：修复 §X.Y 编号** — 修正所有编号错位

- [ ] **步骤 2.3：修复链接文字** — 链接文字与目标 H1 对齐

- [ ] **步骤 2.4：修复 Sidebar** — 增删条目使 sidebar 与实际文件一致

### 任务 3：最终质量验证

- [ ] **步骤 3.1：重新全量扫描** — 提取所有链接并验证

- [ ] **步骤 3.2：报告** — 输出验证结果，零问题方可交付
