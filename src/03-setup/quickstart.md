# 快速上手

> TODO: 本章内容待编写

## 安装 OpenCode

```bash
# macOS
brew install opencode

# 或使用 npm
npm install -g @opencode/cli

# 验证安装
opencode --version
```

## 基本配置

```bash
# 添加 Provider
opencode auth connect

# 查看配置
opencode config show
```

## 第一个任务

```bash
opencode "帮我分析当前目录下的代码结构"
```
