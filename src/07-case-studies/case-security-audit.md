# 案例：安全审计流水线

> 在 CI/CD 中嵌入红队+蓝队全流程安全审计，将渗透测试从"季度专项"升级为"持续活动"。

## 案例概述

传统安全审计的模式是"季度扫描 + 年度渗透测试"——频率低、周期长、发现问题时漏洞可能已在生产环境存在数月。本案例展示如何利用 OpenCode 的 `security-research` 团队，构建一条自动化安全审计流水线，将安全审计嵌入到日常开发流程中。

流水线的核心设计遵循**红蓝对抗**模式：红队阶段负责发现漏洞——自动执行 SQL 注入/XSS/CSRF 等 Web 漏洞扫描、依赖 CVE 检测、配置敏感信息泄露检查；蓝队阶段负责修复验证——根据红队报告自动生成修复方案、配置安全基线、执行二次扫描确认修复生效。两个阶段形成"发现 → 修复 → 验证"的闭环，每次 CI 构建都会触发。

流水线的关键能力是**自动 CVSS 评分**：基于 OWASP 评分标准和上下文信息自动计算严重程度，结合人工复核生成最终的安全决策。设计中还融入了 **STRIDE 威胁建模**方法，确保审计范围对系统关键威胁面的全面覆盖。评估指标方面，本案例重点对比了自动化流水线与传统人工审计的扫描速度、漏洞检出率和修复成功率。

## 内容要点

1. **项目背景** — 目标：Web 应用的全面安全审计。审计范围覆盖代码层、依赖层、配置层和运行时层。理解为什么"持续审计"比"季度审计"更有效。

2. **红队阶段** — 自动漏洞扫描（SQL 注入 / XSS / CSRF）、依赖分析（已知 CVE 检测）、配置审计（硬编码密钥、敏感信息泄露）。红队输出结构化的审计报告，包含漏洞详情、利用路径和 CVSS 评分。

3. **蓝队阶段** — 基于红队报告自动生成修复方案，配置安全基线（如 CSP 头、CORS 策略、认证加固），执行二次扫描确认修复生效。蓝队输出修复验证报告和安全基线文档。

4. **STRIDE 威胁建模** — 在审计实施之前进行威胁建模分析，确保红队测试范围与系统威胁面一致。对每个 STRIDE 类别（欺骗、篡改、抵赖、信息泄露、拒绝服务、权限提升）定义对应的审计用例。

5. **自动化流程设计** — `security-research` 团队配置、Agent 工作流编排、审计报告的 CVSS 评分自动化。展示如何将红蓝审计流水线嵌入 GitHub Actions 等 CI/CD 平台。

6. **效果指标** — 扫描速度 vs 人工、漏洞检出率、误报率、修复成功率。用数据证明自动化安全审计的 ROI。

## 关联章节

- ← [Ch4 工作流实战](../04-workflows/)（Team Mode + `security-research` 团队）
- ← [Ch6 高级话题](../06-advanced/)（安全概念基础）
- ← [案例二：遗留系统现代化](real-world-02.md)（安全审计在遗留系统中的应用）
- → [案例：全流程自动化](case-full-pipeline.md)（安全环节在全流程中的位置）

## 验证标准

- [ ] 文章 ≥ 300 行有效内容
- [ ] 包含红队 + 蓝队的完整流程说明
- [ ] 包含 `security-research` 团队配置
- [ ] 包含 CVSS 评分自动化方案
- [ ] 包含 STRIDE 威胁建模分析
- [ ] 包含可运行的安全扫描命令和配置示例
